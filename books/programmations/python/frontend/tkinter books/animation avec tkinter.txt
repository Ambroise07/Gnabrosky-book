🕘 Animation dans le temps (idée)

Tu peux créer une animation en décalant le bouton toutes les X millisecondes :
python 

def animate
	global pos_x
    
	pos_x += 5
    
	btn.place(x=pos_x, y=100)
    
	root.after(50, animate)  # relancer toutes les 50ms


